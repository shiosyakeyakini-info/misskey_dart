import 'package:freezed_annotation/freezed_annotation.dart';

enum NotificationExportedEntity {
  antenna,
  blocking,
  clip,
  customEmoji,
  favorite,
  following,
  muting,
  note,
  userList,
  unknown,
}
