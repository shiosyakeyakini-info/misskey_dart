import 'package:freezed_annotation/freezed_annotation.dart';

enum I2faRegisterKeyAuthenticatorSelectionAuthenticatorAttachment {
  @JsonValue("cross-platform")
  crossPlatform,
  platform,
  unknown,
}
